[project]
name = "petta-soft"
version = "0.2.0"
description = "Soft unification and goal-directed soft proving layered on the petta library's public surface"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["petta_soft*"]
